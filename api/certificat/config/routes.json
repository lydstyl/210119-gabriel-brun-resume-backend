{
  "routes": [
    {
      "method": "GET",
      "path": "/certificats",
      "handler": "certificat.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/certificats/count",
      "handler": "certificat.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/certificats/:id",
      "handler": "certificat.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/certificats",
      "handler": "certificat.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/certificats/:id",
      "handler": "certificat.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/certificats/:id",
      "handler": "certificat.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
